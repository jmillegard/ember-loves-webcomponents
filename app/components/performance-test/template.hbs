{{yield}}
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button><paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>
<paper-button raised {{action 'signIn'}}>Sign in as {{username}}</paper-button>